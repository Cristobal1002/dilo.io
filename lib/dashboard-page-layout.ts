/** Ancho estándar de páginas del panel (Mis flows, Outreach, cuenta, integraciones). */
export const dashboardPageClass = 'mx-auto w-full max-w-6xl'

/** Tablas anchas (resultados de flow). */
export const dashboardPageWideClass = 'mx-auto w-full max-w-screen-2xl'

/** Formularios enfocados (crear flow, conectores). */
export const dashboardPageNarrowClass = 'mx-auto w-full max-w-3xl'
